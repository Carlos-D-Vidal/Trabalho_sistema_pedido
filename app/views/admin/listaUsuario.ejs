<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listar</title>
    <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
    <%- include('../../public/admin.html') %>
    <h1>Usuarios</h1>
    <table>
        <tr>
            <th>Nome</th>
            <th>Email</th>
        </tr>
        <% for (let i = 0; i < usuario.length;i ++) {%>
        <tr>
            <td><%= usuario[i].nome %></td>
            <td><%= usuario[i].email %></td>
            <td><a id="button" href="/admin/editar/<%= usuario[i].id %>">Editar</a></td>
            <td><a id="button" href="/admin/excluir/<%= usuario[i].id %>">Excluir</a></td>
        </tr>
        <% } %>
    </table>
</body>
</html>