<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista produtos</title>
    <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
    <%- include('../../public/admin.html') %>
    <h1>Produtos</h1>
    <table>
        <tr>
            <th>Descrição</th>
            <th>Preço</th>
        </tr>
        <% for (let i = 0; i < produto.length;i ++) {%>
        <tr>
            <td><%= produto[i].descricao %></td>
            <td><%= produto[i].preco %></td>
            <td><a href="/admin/abrirEditar/<%=produto[i].id %>">Editar</a></td>
            <td><a href="/admin/excluirProduto/<%=produto[i].id %>">Excluir</a></td>
        </tr>
        <% } %>
    </table>
</body>
</html>