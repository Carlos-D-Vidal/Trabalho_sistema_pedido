<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alterar dados</title>
    <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>

<body>
    <% if (erros){ %>
        <ul>
            <% for (let i=0; i < erros.length; i++) { %>
                <li><%= erros[i].msg %></li>
            <% } %>
        </ul>
    <% } %>

    <h1>Alterar dados</h1>
    <form id="form-alterar" action="/usuario/alterarDados" method="post">
        <label>Nome</label>
        <br>
        <input type="text" value="<%= usuario[0].nome %>" name="nome" id="nome">
        <br>
        <label>Email</label>
        <br>
        <input type="email" value="<%= usuario[0].email %>" name="email" id="email">
        <br><br>
        <input type="submit" value="Salvar">
    </form>
</body>
</html>